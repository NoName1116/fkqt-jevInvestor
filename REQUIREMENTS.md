# fkqt-jevInvestor 需求基线

版本：0.4

日期：2026-09-21

状态：用户已确认

架构依据：`docs/specs/2026-09-21-market-factor-jev-llm-design.md`

## 已确认需求

- **R1 独立公开仓库**：新系统使用独立公开 GitHub 仓库 `fkqt-jevInvestor`，Python 包名为 `fkqt_jevinvestor`；不得继续作为 FKQT 仓库内的产品子目录开发。
- **R2 纯 Python 第一版**：第一版使用 Python 3.12、FastAPI、CLI、Pydantic、SQLAlchemy、Alembic、pytest 和官方 `typesafe-sdk`；不包含前端工程。
- **R3 运行范围**：第一版仅运行 A 股日频模拟信号，每个有效交易日 D 收盘后生成只对下一有效交易日 D+1 生效的信号。
- **R4 无券商接入**：系统不连接真实券商，不发送真实订单，只生成信号并按确定性虚拟成交规则维护模拟持仓。
- **R5 FKQT 边界**：FKQT 是外部上游，不是 Python 运行时依赖。新系统只通过版本化 REST API 或不可变文件快照读取候选池、行情、交易日历和证券状态，不导入 FKQT 内部模块，不写入 FKQT 数据库。
- **R6 离线可运行**：正式接收的 FKQT 数据必须冻结为不可变本地快照；回测、记录重放和单元测试不得要求 FKQT 正在运行。
- **R7 LLM 离散决策**：最终决策 LLM 只允许输出 `ENTER`、`KEEP`、`EXIT`、`AVOID`；Provider 不可用或输出无效时标准化为 `NO_SIGNAL`。LLM Schema 禁止包含目标仓位、数量、价格、止损比例、止盈比例或资金比例字段。
- **R8 Jev 定位**：Jev 只读取由代码预先计算的压缩行情特征，输出候选池整体风险状态，以及个股下一交易日盈亏类别、未来 5 个交易日盈亏类别、最大不利波动风险、盈亏不对称性和数据充分度的类型化概率。上述概率是待样本外校准的预测，不得在校准前宣称为真实市场概率。Jev 不计算已实现盈亏、不计算技术指标、不决定交易动作、不决定仓位、不生成订单价格。
- **R9 持仓状态**：每次决策必须读取模拟账户现金、持仓数量、持仓成本、当前权重、浮动盈亏、持有交易日数量、历史信号和未完成虚拟订单。
- **R10 第一版输入边界**：第一版模型输入为行情、交易日历、证券结构化状态、确定性行情特征、当前持仓和历史决策；不建设新闻、公告正文、政策或行业事件 REST 语义采集链路。
- **R11 行情快照审计**：每次正式运行必须保存上游类型、上游版本、请求范围、数据截止时间、候选池版本、原始快照引用、内容哈希、Schema 版本、抓取时间、记录数量和缺失原因。
- **R12 Point-in-Time**：任何用于交易日 D 决策的数据必须满足 `data_time <= decision_cutoff(D)`。日线、交易日历、证券状态、公司行为、特征、模型输入和回测标签均不得包含截止时间之后的信息。
- **R13 结构化交易状态**：有效交易日、停牌、ST/退市风险、涨跌停、上市初期无涨跌幅阶段、除权除息、未复权价格、成交量、成交额、市场和板块标识必须保留为结构化字段。
- **R14 确定性特征**：收益、趋势、动量、反转、波动率、成交量、流动性、跳空和横截面排名必须由版本化代码计算；相同冻结输入必须产生逐字段相同的 Decimal 结果和内容哈希。
- **R15 数据缺失语义**：行情、证券状态或特征缺失必须携带稳定的 `missing_reason`；必要输入不足的证券不得进入模型，不得用零静默填充。
- **R16 时间排期**：D+1 必须来自交易日历，禁止使用自然日加一；交易日历未知时整批拒绝排期。
- **R17 模拟执行**：有效信号在 D+1 开盘按可成交价格虚拟执行，并计入手续费、滑点、停牌、涨跌停、T+1 和容量限制。
- **R18 信号有效期**：信号只对指定 D+1 有效；无法成交时记录终态失败原因，不自动跨日追单，除非下一次正式决策重新产生可执行目标。
- **R19 确定性仓位**：目标仓位和目标数量只能由版本化仓位引擎根据离散动作、波动率、流动性、组合约束、现金和 A 股整手规则计算；相同输入必须产生完全相同输出。
- **R20 风险约束边界**：确定性规则可以阻止结构无效、数据不足、资金不足、负数仓位、超出单票或总仓位上限和不可成交信号，但不得把 `ENTER` 反转成 `EXIT`，也不得把 `EXIT` 反转成 `ENTER`。
- **R21 全链路审计**：每次决策必须保存候选池、行情快照、证券状态、特征及版本、缺失原因、Jev 请求与概率、LLM 输入与原始响应、标准化动作、仓位引擎版本与输入、决策前持仓、目标仓位、虚拟订单和成交结果的引用或不可变记录。
- **R22 可重放性**：系统必须能从已冻结的候选池、行情快照、特征、正式模型输出和仓位输入重建当日记录；记录重放不得重新调用外部 Provider，模型重评必须创建独立实验记录且不得覆盖原结果。
- **R23 模型可替换**：Jev 和最终决策 LLM 均通过 Provider 接口接入，业务代码不得绑定单一 SDK、Base URL 或模型名称。
- **R24 Jev 失败**：没有有效 `TYPESAFE_API_KEY`、账号未获权限、超时或 API 不可用时，Jev 实验组记录 `PROVIDER_UNAVAILABLE`，不得写入伪造概率，也不得悄悄退化为无 Jev 实验组。
- **R25 LLM 失败**：最终决策 LLM 不可用、响应不可解析或超时时，B/C 组输出 `NO_SIGNAL` 并保持原持仓，不调用备用模型静默替代。
- **R26 四组对照实验**：支持 A“确定性规则→仓位引擎”、B“确定性特征→LLM→仓位引擎”、C“确定性特征→Jev 盈亏概率→LLM→仓位引擎”、D“确定性特征→Jev 盈亏概率→确定性动作映射→仓位引擎”四条互斥链路，并固定候选队列、行情快照、截止时间、交易成本、仓位公式和评估区间。C 组是第一版主链和优先交付对象；A/B/D 只用于消融与基线比较。Jev 不得直接输出 `ENTER`、`KEEP`、`EXIT` 或 `AVOID`。
- **R27 候选池约束**：第一版使用 FKQT 提供并已冻结版本的 A 股候选队列，默认目标数量为 80 只，不允许 Jev 或 LLM 自由构造证券代码。合格证券少于 80 只时保留实际数量并记录原因，不得用不合格证券补齐；队列成员变化必须产生新的版本和哈希，禁止回写历史队列。
- **R28 输出完整性**：每次运行必须对候选池产生可验证结果，包括离散动作、无信号、数据不足、跳过或失败状态，禁止静默遗漏。
- **R29 样本外门槛**：Jev 和 LLM 只有在样本外评估中相对确定性基线产生可重复增量才保留；Schema 合法、叙述合理或单次盈利不能作为有效性证据。
- **R30 迁移原则**：已完成并通过测试的 Phase 0/1 Python 代码按功能迁移，不重新实现；迁移前后使用同一 Fixture 验证账本、费用、T+1、幂等和资产恒等式。
- **R31 旧系统保护**：迁移验证完成前保留 FKQT 工作树中的 `ai-signal-lab`；验证完成并获得删除确认后才能移除。不得修改 FKQT 旧策略、旧模拟账户、LLM Overlay 和 Forward Test 行为。
- **R32 协作流程**：`main` 分支只接受 Pull Request；合并前必须通过自动化检查和至少 1 名非作者 Review；API Key、数据库凭据和模型 Secret 不得进入 Git 历史。
- **R33 测试节制**：每个任务运行最小失败测试和对应通过测试；修复后只重跑失败项；完整离线套件、静态检查和迁移往返仅在阶段最终验收运行一次。
- **R34 回测协作边界**：回测实现由外部 Contributor 沙耶负责；核心项目先冻结 `backtest-contract-v1`，她只修改 `src/fkqt_jevinvestor/backtest/`、`tests/backtest/` 和 `docs/runbooks/backtesting.md`，不得自行修改行情、特征、仓位、执行和持仓核心 Entity。
- **R35 回测输入输出**：回测引擎通过 `ReplayDataProvider`、`TargetProvider` 和 `ExecutionPort` 协议消费冻结快照与目标仓位批次，输出逐日记录和汇总指标；不得直接调用 FKQT、Jev、LLM、FastAPI 或 SQLAlchemy Repository。
- **R36 防前视与可比性**：回测决策只读取 D 日截止时间前的冻结输入，成交只使用交易日历确认的 D+1 执行快照；A/B/C/D 组共享数据集、初始资金、仓位版本、成本和执行规则。
- **R37 Jev 双层输入**：Phase 3 必须分别定义候选池级 `JevUniverseStateV1` 和个股级 `JevSymbolStateV1`。候选池级状态在同一候选池版本、决策日和截止时间下最多正式调用一次并供全部个股复用；个股级状态按证券独立调用。两者不得混成每只证券重复提交的单一状态。候选池级判断不得表述为全 A 股市场状态。
- **R38 Jev 输入隔离**：Jev 输入只允许包含身份与审计字段、代码计算的确定性特征、横截面位置、结构化证券状态、缺失与覆盖信息。不得包含原始长 K 线、新闻正文、未来标签、目标仓位、现金、持仓成本、浮动盈亏或历史模型决策。
- **R39 Jev 输出边界**：Phase 3 只使用有限标签的 `Choice` 概率分布。每个问题必须保存全部标签概率和派生的最高概率标签；不得输出连续收益率、连续因子权重、目标仓位、目标价、止盈止损比例或直接交易动作。预测问题的交易日跨度、标签阈值、费用口径和标签生成公式必须由版本化代码固定。
- **R40 Jev 版本与失败审计**：每次 Jev 调用必须保存 `model_id`、`provider_version`、`state_schema_version`、`question_version`、`criteria_version`、输入哈希、原始响应哈希、延迟和调用状态。`PROVIDER_UNAVAILABLE`、`CONTRACT_INVALID`、`DATA_UNAVAILABLE` 必须独立于概率分布记录，失败时不得合成概率。
- **R41 因子权重删除**：第一版不实现动态因子权重、权重 Profile 或概率到权重的映射；Jev 和最终决策 LLM 均不得生成连续因子权重。重新引入该能力必须新增需求并独立评审。
- **R42 盈亏标签归属**：用于校准 Jev 的实际盈亏、最大不利波动和盈亏不对称标签必须由代码从 D+1 起的冻结执行行情生成；Jev 不得参与标签计算。正式前向决策不得读取这些未来标签。
- **R43 队列外持仓**：正式决策证券集合为“冻结候选队列与当前持仓证券的并集”。不在候选队列但仍有持仓的证券标记为 `HELD_ONLY`，不计入默认 80 只候选名额，只允许最终 LLM 输出 `KEEP`、`EXIT` 或 `NO_SIGNAL`；系统不得因证券掉出候选队列而静默遗漏或自动清仓。

## 明确非目标

- 不接入真实券商，不发送真实订单。
- 不做分钟级或 Tick 级交易。
- 不让 Jev 或 LLM 计算技术指标、资金余额、持仓成本、成交价格、目标仓位或订单数量。
- 不在第一版建设新闻、公告正文、政策和行业事件的语义资料层。
- 不在第一版训练自有模型。
- 不在第一版开发 Web 前端或桌面客户端。
- 不保证 Jev 或 LLM 产生正收益。
- 不复制 FKQT 行情数据库，不绑定 FKQT 内部 Python 模块。

## 验收原则

需求变更必须新增或修改编号条目。任何代码改动必须映射到至少一个需求编号；无法映射的改动不得进入项目。
